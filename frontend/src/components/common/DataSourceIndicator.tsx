import React from 'react';

interface DataSourceIndicatorProps {
  compact?: boolean;
}

export const DataSourceIndicator: React.FC<DataSourceIndicatorProps> = ({ compact = false }) => {
  return null;
};

export default DataSourceIndicator;